[package]
name = "workers-society-room"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
libhinemos-room = { path = "../libhinemos-room" }

[dev-dependencies]
ron = "0.12.1"
serde = { version = "1.0.228", features = ["derive"] }
